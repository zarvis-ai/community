============
zarvis.yaml
============

## Overview

all configurations with default value

.. code-block:: yaml

    apiVersion: beta/v1
    deploy:
      skaffold:
    ingress:  
    - service:
        port:
    auth:
      type: ZARVIS  # ZARVIS (default) or ANON
